<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Laser Technology Quiz</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/css/style.css">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="min-h-screen bg-gray-50">
    <nav class="bg-indigo-600 text-white shadow-md">
        <div class="container mx-auto px-4 py-3 flex justify-between items-center">
            <div class="flex items-center space-x-2">
                <i class="fas fa-laser-pointer text-2xl"></i>
                <span class="text-xl font-bold">Laser Tech Quiz</span>
            </div>
            <% if (typeof user !== 'undefined') { %>
                <div class="flex items-center space-x-4">
                    <span class="hidden md:inline"><%= user.name %></span>
                    <a href="/logout" class="py-2 px-4 bg-red-500 hover:bg-red-600 rounded-lg text-sm transition duration-300">
                        <i class="fas fa-sign-out-alt mr-1"></i>
                        <span class="hidden md:inline">Logout</span>
                    </a>
                </div>
            <% } %>
        </div>
    </nav>
    <div class="container mx-auto px-4 py-8"> 